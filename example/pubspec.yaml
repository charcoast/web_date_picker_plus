name: example
description: The project example for web date picker

publish_to: 'none' 

version: 1.0.0

environment:
  sdk: ">=2.17.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_date_picker_plus: 
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true